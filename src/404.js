import React from 'react';
import ReactDOM from 'react-dom/client';
import NotFound from './components/NotFound';

const root = ReactDOM.createRoot(document.getElementById('notfound'))

root.render(
  <React.StrictMode>
    <NotFound />
  </React.StrictMode>
  
);
